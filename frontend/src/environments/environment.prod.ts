export const environment = {
  production: true,
  apiUrl: '',  // URLs relativas: /api/v1/* vai para o mesmo host (nginx faz proxy)
  supportEmail: 'suporte@dropshipping.com',
  supportWhatsapp: '+5511999999999',
};
